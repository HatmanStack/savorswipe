// Expo Router entry point.
// This file re-exports expo-router/entry so that the `main` field in
// package.json resolves to a local file, which keeps static analysis
// tools (e.g. knip) happy while preserving Expo Router's runtime behavior.
import 'expo-router/entry';
